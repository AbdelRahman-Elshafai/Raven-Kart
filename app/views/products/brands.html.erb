<% content_for :title, "Brands" %>

<% @brands.each do |brand|%>
    <h2><%= brand[0]%></h2>
    <br>
    <div class="row">
    <% brand[1].each do |product| %>
    <%= render 'products/productcard', product: product%>
    <% end %>
    </div>
    <br>
<% end %>
